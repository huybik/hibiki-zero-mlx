# Qwen MLX compaction v5 erratum

Date: 2026-08-04. This note preserves the immutable v5 result as `NO-GO`; it does not rewrite or relabel any v5 artifact.

## Selection gate

`select_vivos_qwen_compaction_v5.py` excluded waveform, duration, prompt-leak, and speaker failures but accidentally ignored the frozen per-row English-ASR gate (`WER <= 0.50`). Consequently, immutable `selection_rows.jsonl` (SHA-256 `b5fe9cb3977aa4ded0265cde41f117857f070d38df7e9d76eea4688b99efd78c`) accepted:

- `vivos:train:VIVOSSPK08_R108`, attempt 1: 5/2 word errors, WER 2.5000.
- `vivos:train:VIVOSSPK08_R200`, attempt 0: 2/3 word errors, WER 0.6667.

The affected aggregate is `selection_report.json` (SHA-256 `bcb3b64bf59de377b6d6e980b09e7c79eb749ecba9175c2e31f2fdcce663790b`): 58 accepted, 83/807 errors, WER 0.10285. Applying every frozen row gate to the same preserved attempts yields 56 accepted, 76/802 errors, WER 0.09476. It remains above 0.08, so the scientific conclusion remains `NO-GO`.

The shared final-selection boundary in `qa_vivos_full.py` now admits only candidates passing every frozen row gate and chooses minimum integer word errors, then row WER, then immutable attempt order.

## Effective repetition penalty

The frozen external `benchmark_plan.json` (SHA-256 `437b6d3d9187bfa8f9463a517840d581859bc12880e6b18dca88878a8785e64e`) inherited installed `Model.batch_generate` metadata stating effective ICL repetition penalty 1.5. That statement is correct only for the installed-path `global_b8` control. Every repository-owned optimized candidate in `raw_timing.jsonl` directly used `qwen_mlx_compaction.generate_lanes`, which applied the requested penalty **1.05** without the installed-path clamp.

The affected results are all 13 non-`global_b8` entries in `raw_timing.jsonl`: `row_rng_b8`, `row_rng_length_b8`, `row_rng_length_compact_b8`, and `row_rng_length_compact_recurrent_b8` across their recorded throughput64, quality16, quality64, temperature-0.7, and attempt-1 retry executions. Their candidate JSON, WAVs, codes, hashes, timings, QA, and selections are unchanged; only the effective-configuration interpretation is corrected to 1.05.

# Hibiki MLX — project context

MLX runtime for Kyutai's **Hibiki-Zero** (simultaneous speech-to-speech + speech-to-text translation, FR/ES/PT/DE → EN, 3B hierarchical transformer + Mimi codec @ 12.5 Hz) on Apple Silicon, aimed at the **iPhone 80 ms/frame budget**. The upstream PyTorch stack was purged; the MLX q4 path is the product. **All 5 phases of `docs/refactor_plan.md` are done**: P1 restructure, P2 inference sweep (`reports/inference_matrix.md`), P3 Track B parallel head *scaffold* (`reports/parallel_head_smoke.md` — AR head stays default; parallel needs a scaled run before it ships), P4 finetune refactor (`docs/finetune.md`), P5 iPhone-readiness (phone artifact + `scripts/check_swift_compat.py` + `reports/iphone_budget.md`). Chosen phone config: **Hibiki-M 1B q4** (FR→EN), 3B stays the Mac/teacher model.

- Upstream: https://github.com/kyutai-labs/hibiki-zero · https://arxiv.org/abs/2602.11072v1 · weights CC BY-NC-SA 4.0.
- Python env: conda base at `/opt/homebrew/Caskroom/miniconda/base/bin/python` (py3.13; mlx 0.31, torch 2.13, moshi 0.2.13, rustymimi 0.4.1; `moshi-mlx/` and `hibiki_mlx` installed editable).

## Layout (repo root = `code/`)
- `hibiki_mlx/` — THE runtime package (installed editable). `pipeline.py` = pipelined q4 inference `load(dir, quant)`/`run()`/`make_mimi()`/`resolve_weights_dir("3b"|"1b"|path)`; honors each model's config (dep_q, n_q, delays, conditioners). No `sys.path` hacks; weights anchor on repo root (`hibiki_mlx.pipeline.W = <root>/weights`, `MODEL_DIRS`).
- `main.py` — thin CLI: `python main.py <audio> [--model 3b|1b] [--quant q4|q4-depq3]` (file → `translations/<stem>_translated.wav` + `.txt`, 3-thread pipelined) or `python main.py --mic` (encoder/LM/decoder threads; live critical path = LM step: 3B ~22 ms, 1B ~15 ms on M4).
- `moshi-mlx/` — **vendored fork of `moshi-mlx` 0.3.0** (installed editable). Owns all hibiki-zero deltas: `hidden_scale=6` + `kv_repeat=2` (GQA) + `rope_concat` + the learned per-slice depformer output **LayerNorm** (`depformer_norms.{i}`) — without the LayerNorm the audio babbles (silence-in test: correct model rms≈0.06, buggy rms≈0.13 peak≈1.23). Also handles Hibiki-M's float `hidden_scale=4.125`. Phase-2 deltas: `DepFormer.sample` fast path (cfg_coef==1) threads KV functionally through one `mx.compile`d step per slice; `make_rot_cache` caps `RotatingKVCache` at `cfg.context` (KV at cap: 3B 344 MB / 4 min window, 1B 66 MB / 40 s). **q4 must be regenerated after touching the depformer** (LayerNorm stays bf16; nn.quantize skips non-Linear). Runs on mlx 0.31 despite the fork's `<0.27` pin.
- `scripts/` — conversion/verification/benchmark only: `convert_mlx_q4.py` (PyTorch LM → `weights/hibiki.q4.safetensors`, `nn.quantize(bits=4, group_size=32)`), `convert_hibiki_m_mlx_q4.py` (stages `weights/hibiki-m-mlx-q4/`, 1.125 GB q4 LM), `convert_depq3.py` (q4-depq3 variants), `verify_mlx_q4.py` (gate: leon.wav → `translations/leon_mlx_q4.wav`), `bench.py` (per-stage ms / RT / size / iPhone projection / `--silence` gate), `check_swift_compat.py` (device-free: validates a staged artifact the way `moshi_mlx.run_inference`/moshi-swift load it — gs32 q4 naming/shapes via strict reload, config keys, tokenizer+mimi load), `push_mlx_q4.py`, `push_hibiki_m_mlx_q4.py`. **Keep `group_size=32`** — stock moshi-mlx/moshi-swift hardcode gs32 for `.q4.safetensors`.
- `assets/samples/` — test clips: `leon.wav` (~64 s FR Olympics), `crepes.mp3`, 12 s cuts.
- `weights/` (gitignored) — 3B checkpoint (`config.json`, `hibiki-pytorch-77f82164@110.safetensors` 5.8 GB, `hibiki.q4.safetensors` 2.2 GB, mimi 367 MB, tokenizer) + `hibiki-m-mlx-{bf16,q4}/`.
- `finetune/` — PyTorch LoRA training stack (uses the `moshi` pip package; PyTorch eval helpers live in `finetune/hibiki_helpers.py`, relocated from the deleted upstream `hibiki_zero` package).
- `remote_dataset/` — CoVoST2 + FLEURS eval data & scripts. `training-data/` — PhoMT TTS pipeline. `docs/`, `vision/`.
- Published artifacts: [`huybik/hibiki-zero-3b-mlx-q4`](https://huggingface.co/huybik/hibiki-zero-3b-mlx-q4) (weights + portable stock-pkg patch shim), [`huybik/hibiki-1b-mlx-q4`](https://huggingface.co/huybik/hibiki-1b-mlx-q4) `@9649ed0`.

## Runtime facts (M4 Pro; full matrix in `reports/inference_matrix.md`)
- Pipelined file path **3B ~3.0× / 1B ~4.1× RT**: encoder thread streams `encode_step` ahead → main-thread GPU LM step → decoder thread. rustymimi releases the GIL; each thread needs its **own** `rustymimi.Tokenizer` ("Already borrowed" panic otherwise). Codec threads queue **numpy** (not mx) arrays — lazy mx graphs are bound to the creating thread's stream and mlx ≥0.27 refuses cross-thread eval.
- Per-frame GPU LM: **3B q4 ≈ 22 ms** (main 11.9 + depformer 10.2), **1B q4 ≈ 14.7 ms** (main 7.4 + depformer 7.3); mimi codec (CPU) enc ~18 / dec ~17 ms, fully hidden by the pipeline. Depformer stays **sequential-launch-bound** — the compiled per-slice fast path (Phase 2) bought −18% dep / −9% frame on the 3B, quantization buys 0 on M4; only the parallel-codebook head (Phase 3 / distill plan) removes the serial passes. Projected iPhone LM step at 0.5× M4 scale: 1B ~29 ms, 3B ~44 ms vs the 80 ms budget.
- Quant variants: `q4-depq3` (slice transformers q3, emb/linear_out stay q4) = 3B 2.21 GB / 1B 1.05 GB, same speed, small BLEU cost (1B 28.4→26.8). Quantizing depformer emb+linear_out to q3 makes the 3B babble-loop through the tail flush — don't.
- Tail flush: input ends → feed `tail_s` (8 s) silence to flush the ~6 s translation lag, early-stop after `PAD_STOP`=12 pad frames (sitting longer hallucinates). Text temp **0.4** default (audio 0.8): kills spurious cold-start openers, BLEU 22.1→25.8 (text feeds back autoregressively; greedy 0.0 over-collapses).
- Gates for runtime changes: `python scripts/verify_mlx_q4.py` coherent, and silence-in via `scripts/bench.py --model {3b,1b} --silence` (10 s zeros → rms < 0.10, peak < 1.1; healthy: rms≈0.0002, peak≈0.012; exact value is seed-dependent).
- Codec-fidelity: decoding generated tokens at fewer codebooks degrades gracefully (mel-dist 0.049@8, 0.105@4). Inference-time depformer truncation is NOT a faithful dep_q=8 preview (feedback goes OOD).

## Phone artifact + iPhone budget (Phase 5)
`weights/hibiki-m-mlx-q4/` is the staged standalone phone artifact (config.json `moshi_name`→q4 file, `hibiki-mlx-dc2cf5a5@80.q4.safetensors` 1.13 GB gs32, `.q4-depq3` 1.05 GB variant staged alongside via `convert_depq3.py --model 1b`, mimi 0.38 GB, tokenizer, model card). Ships the **AR head**. `scripts/check_swift_compat.py weights/hibiki-m-mlx-q4/` → **7/7 PASS** (config keys, `moshi_name`.q4, gs32 on 298 tensors, strict gs32 q4 reload == `run_inference.py`, tokenizer 48k, mimi nq=8). The **depq3 variant is our-pipeline-only** — its name isn't `.q4.safetensors`, so stock moshi-swift wouldn't q4-load it; the q4 file is THE swift-compatible one. Publish is a manual authed step (`push_hibiki_m_mlx_q4.py`, not re-pushed here).
- **Budget (`reports/iphone_budget.md`, projections — no device):** scale = A18/M4-Pro GPU throughput, default 0.5× (band 0.4–0.6). M4 LM step: 1B AR 15.1 ms (main 8.0 + dep 7.0), 3B AR 22.2 ms (12.3 + 9.9), 1B+parallel ~13.4 ms (par head 5.3 ms measured on 3B, conservative). Projected iPhone @0.5×: **1B AR 30 ms, 1B parallel 27 ms, 3B AR 44 ms — all FIT 80 ms**; worst case @0.4× 1B AR ~38 ms. Memory (AR): 1B ~1.8 GB (LM 1.13 + KV 0.07 + mimi 0.38 + ~0.2 act), 3B ~3.4 GB. 1B fits a base-6 GB iPhone app budget (~2.5–3 GB); 3B needs 8 GB Pro + extended-memory entitlement. **Blocking true realtime (device-side, honest):** no moshi-swift on-device build in loop; ANE-vs-GPU placement (vision targets ANE, current path is Metal GPU; AR depformer maps poorly to ANE → parallel head); sustained thermals; launch-bound dispatch may scale worse than bandwidth on a phone GPU.

## Benchmarks
- `remote_dataset/download_covost2.py` → wav + manifest; `run_batch.py` (loads LM once, `--model 3b|1b --quant ...`) → text sidecars; `evaluate_translation_text.py` → BLEU/chrF/WER. **CoVoST2 fr→en n=30: 3B q4 BLEU 25.7 / chrF 49.4; 1B q4 BLEU 28.4 / chrF 54.6** (Hibiki-M is the dedicated FR→EN model; paper 3B 30.6). If `HF_HOME` points at a dead mount: `export HF_HOME="$(pwd)/.hf_cache"`.
- FLEURS vi↔en (`remote_dataset/fleurs_vi_en/`, downloader joins vi_vn+en_us on FLoRes id): train 1449 / val 149 / test 347 pairs (vi 6.19 h). **vi→en baseline ≈ zero** (BLEU 0.26) — vi is unsupported, needs training.

## Track B — parallel codebook head (`distill/`, Phase 3 scaffold)
Self-distill a **parallel** head to replace the AR depformer's 16 sequential launch-bound slices (distill_plan §3–9). Frozen main = teacher; only the new head trains; keep cb16 (§6). **Scaffold + smoke distill done; quality needs a scaled run — AR head is the default, parallel is opt-in.**
- `distill/dump_teacher.py` — inference-only, resumable shards of `(transformer_out fp16, text token, N teacher tokens, N×vocab teacher logits fp16)` via a capture hook on `DepFormer.sample` (`model.depformer.capture=True` → `.last_logits/.last_tokens`). Smoke: 30 CoVoST2 fr clips / 2289 frames / 152 MB (~66 KB/frame; ~2.9 GB/h — go top-k past ~5 h).
- `distill/parallel_head.py` — vectorised delay-pattern head: per-codebook `w_in/w_out/cb_emb/norm` stacked, applied by batched einsum/gather (few launches, not 370); shared 6-layer bidirectional trunk over the 16 codebook positions; codebook k conditions on its own prev-frame token (delay-1). `num_passes` knob (1 default; 2–4 iterative-refine placeholder). Warm-start from AR depformer where shapes match. 227 M params bf16.
- `distill/train_head.py` — `KL(teacher‖student,T)+CE`, Adam, `value_and_grad` on **head params only**, clip-clean split, head-only checkpoint. Smoke: full-train loss 21.4→2.9 over 500 steps; overfit-32-frames → 0.044 (capacity/grad sanity).
- Integration: `HIBIKI_HEAD=parallel [HIBIKI_HEAD_CKPT=… HIBIKI_HEAD_PASSES=N]` → `pipeline.load()` attaches the bf16 head (after `nn.quantize`, so head stays bf16); `Lm._sample` routes to `parallel_head.sample` (owns its prev-token streaming state, reset after warmup); `bench.py` times it.
- **Measured (3B, M4, clean):** codebook head **12.70→5.28 ms (−58%)**, LM total **25.9→18.5 ms (−29%)**, 3.1→4.3× RT live. Head is now bandwidth-bound (227 M bf16) not launch-bound → q4 head + param-shrink is the path to the 1–4 ms target. Gates: AR default still coherent + silence-in PASS (rms 0.0002); parallel path text coherent (text bypasses the head), silence-in PASS (rms 0.064) even at smoke scale.

## Finetune (Track A, Vietnamese) — Phase 4 done
`finetune/`: `build_pairs.py` (FLEURS → `pairs/{split}.jsonl` + deterministic `val16`/`val128` gate subsets) **and `fetch_phomt.py` (HF `anquachdev/PhoMT-en-vi-speech` → `remote_dataset/phomt_en_vi/` wavs + `pairs/phomt_train.jsonl`; enumerates all `data/*.parquet` via `list_repo_files` — the dataset scaled to 148k rows / 224 VI-h measured on 2026-07-22 — `--limit N` caps pairs, vi>25 s dropped)** → `cache_codes.py` (Mimi/text codes, coarse target delay; PhoMT → `cache/phomt_train/`) → `train_lora.py` (**`--cache-dir` takes multiple dirs; shards pooled, e.g. FLEURS `cache/train` + `cache/phomt_train` = 2438 samples, no FLEURS re-encode**; `--max-frames N` drops over-long cached samples — use **280** (22 s) to avoid the long-tail MPS OOM) → `validate_lora.py` / `eval_lora.py` (batched greedy eval, loop metrics, BLEU/chrF/WER) → `autoresearch.py` (fixed trial harness, TSV protocol, primary metric val chrF). **All shared logic (model+LoRA build, adapter save/load, cached-shard dataset, teacher-forced losses, greedy generation, metrics, schedule primitives) lives once in `finetune/common.py`**; the three scripts are thin wrappers (eval_lora 670→141, validate_lora 191→106 lines). See `docs/finetune.md`.
- Freeze map: LoRA on `LMModel.transformer` + optional full `text_linear` (`--train-text-head`) + audio-head LoRA on `depformer_in`/`linears` (`--train-audio-heads`), zero-init new LoRA (random-init spikes loss). Supervise prefix-delay text pads, mask tail padding; text loss weight ~5. **`--full-finetune`** = paper-faithful full-model SFT (unfreeze all, single LR group, `model_step*.safetensors` `target=full`); it's the scaled CUDA run (won't fit MPS) — **use `--dtype float32` (bf16 master weights round away lr~1e-5 Adam updates; on CUDA the forward runs under bf16 autocast + fused AdamW automatically) and `--keep-checkpoints` (default 2) since full saves are ~35 GB (model + AdamW state)**. LoRA stays the MPS plumbing probe; `eval_lora`/`validate_lora` auto-detect via `common.load_finetuned`.
- **Schedules (Phase 4, piecewise `value@fraction` of total steps; static flags = single point):** loss weights `--text-weight-schedule "5@0,2@0.6"` / `--audio-weight-schedule`; replay `--replay-weight-schedule "300@0,100@0.5"` (rebuilds the replay sampler at boundaries, needs `--replay-ids`); per-group LR `--lr-schedule` / `--text-head-lr-schedule` / `--audio-head-lr-schedule` + `--warmup-steps`. **Selection:** `--eval-every N` runs batched greedy val eval (default val128) and saves `adapter_best.safetensors` on chrF improvement. All `torch.mps.*` gated behind `is_mps(device)` → CUDA runs clean.
- Findings so far: best 16-row result 11/16 exact (`vn_lora_short16_audioheads_lr1e4_from_s800`); staged replay 300→100 is best fixed-gate row (chrF 9.20, seen3 3/3) but val output still loops — not real translation yet. Protocol: seen3/short16 smoke-only, **val128 for real decisions**, full 1449 last; 5/16-row deltas are noise. MPS train `--dtype bfloat16 --batch-size 2 --grad-accum-steps 2` (batch 4 spikes to 47.5 GB); eval batch 8. One hypothesis per commit, keep only if val chrF improves.
- **MPS memory gotcha:** the pool has 262 distinct seq-lengths; MPS caches a Metal kernel graph per shape → wired GPU footprint balloons to 26 GB (phys peak 48 GB) and swap-thrashes even though torch reports ~9.6 GB. `collate_cached` **buckets frame length to a multiple of 32** (`FRAME_BUCKET`, 9 shapes) → phys peak 48→19 GB, ~9 s/step; loss-neutral (pad = -1 = zero_token_id, masked). CUDA is unaffected. Resume uses `torch.load(weights_only=False)` (checkpoint args hold Path objects).
- **PhoMT+FLEURS combined run `finetune/runs/vn_phomt_combined/` (rank 32, lr 1e-4→3e-5, text-weight 5→2, text+audio heads): BSOD'd at step 605 ≈ end of epoch 1** (609 steps/epoch @ eff-batch 4; `sort_by_length=true` → longest clips up to 51 s in the final batches → MPS 8.6→37.7 GB, over the 36 GB cap → panic; hence `--max-frames 280`). Latest checkpoint step 500; its val128 greedy is empty/looping English ("the idea of the idea of…"), **not translating**.
- **Diagnosis (paper §4.6/§4.2.3, see `docs/vi_training_plan.md`): recipe matches theirs (SFT + CE text/audio + randomized coarse delay) — two gaps: data scale (~7.8 h vs their 850 h floor, ~100×) and method (paper does full-model SFT; we ran LoRA r32).** Fix = scale synthetic speech from the full PhoMT text corpus (~2.9M pairs, only 992 synthesized) to 50–150 h + real vi source (CommonVoice/VIVOS), then **full-model SFT (`--full-finetune`) on CUDA** — LoRA r32 has no evidence of capacity for a new source language, and full finetune removes that confound. Distill+RL are downstream, later.
- In-training greedy eval (`--eval-every`) is too slow on MPS at val128 (each batch decodes full-length pre-EOS) — prefer `--eval-every 0` then a final `eval_lora.py --stop-on-eos`.
- **Plumbing probe verdict (`runs/vn_plumbing_lora`, LoRA r32, stopped step 1750/ep6): pipeline sound — data starvation + LoRA capacity, not a bug.** The ~0.21 train text_loss is pad-dominated (57% of supervised text tokens are prefix pads, CE 0.007); real **content CE 1.08 (ppl ~3), not memorized**. Teacher-forced reconstruction on the *training codes* is right in the tail but **wrong at the sequence start** (the source-grounded tokens) → model learned target-side LM, not VI-source→EN grounding → greedy hallucinates. Not a train↔eval mismatch (errors show in pure TF). Empirically justifies the full-finetune switch (LoRA on frozen backbone can't route a new source language). Next = scale data + `--full-finetune` on CUDA.
- **H100 full-model SFT (vast.ai, 2026-07-29, 3 epochs / 55,284 steps @ batch 8 on 147,421 cached samples = PhoMT 222 VI-h + FLEURS train): val128 chrF 9.2 → 19.6 — full SFT routes VI where LoRA couldn't; quality is now data-limited.** Standalone `eval_lora.py --stop-on-eos` on `model_best` (= final step): **chrF 19.61 / BLEU 0.22, 0 empty, 126/128 EOS, 8/128 looping; residual flaw = 1.9× over-generation**. Trajectory: chrF 9.1 (generic fluent English) → 7.7 dip (grounding transition, val CE spiked 2.5→3.7 = TTS-voice overfit) → 16.4 after LR decay + text-weight drop → 18.6 final, val CE still falling at termination → **more epochs + more data help; late-run chrF is volatile (16→7.7→15.6→18.6), give phase 2 a longer low-LR tail**. Setup: torch 2.12.1+cu126, `NO_TORCH_COMPILE=1` baked into `common.py` (moshi's CUDA lazy compile breaks autograd), lr `1e-4@0,3e-5@0.5` + 500 warmup, `--text-weight-schedule 5@0,2@0.6`, `--val-every 2000 --eval-every 9000 --save-every 5000 --keep-checkpoints 2`; ~0.2 s/step, 59 GB VRAM, 3 epochs ≈ 4.5 h ≈ $9. Artifacts on HF **`huybik/hibiki-zero-vi-full-sft`** (public): best+final checkpoint pair (model 12.5 GB + AdamW 25 GB), `mimi_caches.tar.gz` (all Mimi caches + pairs, 726 MB — the 224 h never needs re-fetch/re-encode), logs, val128 predictions. `finetune/hf_sync.py` = checkpoint-sync loop (HF private storage counts LFS *history* — must delete+`super_squash_history` before upload; repo made public to lift quota). Caching: 8-way parallel `cache_codes.py` (split pairs `lines[i::8]`, per-chunk out-dirs, trainer pools via multi `--cache-dir`) ≈ 40 min/224 h on the H100. `fetch_phomt.py --resume-from pairs.jsonl` fetches only new dataset rows (keyed on texts+durations, survives HF resharding). **Phase 2 (1k-h target): use the self-contained Mac-built full cache (below) + FLEURS train, warm-start from best checkpoint (`--init-adapter`), fresh warmup+decay; prioritize voice diversity / real VI speech over more single-voice Kokoro (train/val divergence was the mid-run failure mode).**
- **Full-set Mimi cache (Mac, DONE 2026-08-02, on the Hub):** `cache_phomt_stream.py --device mps --keep-parquet` streamed all Hub rows into `finetune/cache/phomt_stream/` — **1,377 shards / 694,422 samples / 1,114 VI-h** (1,821 rows dropped by the 25 s filter), 6.3 GB int32, ids `phomt_s{shard}r{row}` — self-contained, supersedes the old `phomt_train` cache. Final cacher config: **single worker**, producer-thread prefetch + batched encode (shard-wide length sort, 2M-sample batch budget, pow2 batch sizes, 16-frame pad buckets — the pow2/bucket shapes bound MPS per-shape Metal kernel-graph RSS to ~8–10 GB), ~155 shards/h ≈ 275× RT; MPS encode bit-exact vs CPU (torch 2.13). Degenerate-row scan across all shards: **0 flagged**. Pushed as 8 zstd tar chunks + `pairs_w*.jsonl` + README (3.7 GB) to **`huybik/hibiki-zero-vi-full-sft`** (a public **dataset** repo — the phase-1 checkpoints live untouched on the same-named **model** repo; restore = `hf download --max-workers 16` + untar, minutes on the box). The 565 GB parquet stays archived at `HF_HOME=/Volumes/data/huggingface` (campaign wavs deleted 2026-08-02 after Hub verification); local staging copy of the chunks at `/Volumes/data/phomt_cache_push/`. MLX Mimi was benchmarked as an alternative encoder: 0.74× torch-MPS speed and only ~42% code agreement (semantic codebook flips near boundaries) — never mix backends in one cache.
- **Phase-2 run `finetune/runs/vi_full_p2` LAUNCHED 2026-08-02 on a fresh H100 NVL 94 GB pod (torch 2.13.0+cu132, driver 595/CUDA 13.2); `finetune/phase2.sh` = restore|train launcher.** Data: full 1377-shard cache + FLEURS train; warm-start `--init-adapter model_step055284`; batch 16, `--max-frames 280`, 2 epochs ≈ 85k steps ≈ 7 h at ~0.22–0.28 s/step; lr `5e-5@0,2e-5@0.5,1e-5@0.8` + 500 warmup, text-weight `3@0,2@0.5`; val CE every 2k, val128 greedy every 9k with best-on-chrF; `hf_sync` pushes checkpoint pairs to the model repo (with `hf_transfer` — a single HF connection throttles at ~16 MB/s). Smoke gate passed incl. greedy eval under compile (chrF 22.8 on 8 rows = warm start intact). **Epoch 3 extension (user decision 2026-08-02), chained after the 2-epoch run:** rerun the phase2.sh train command with `--resume-checkpoint finetune/runs/vi_full_p2/trainer_step086258.pt --epochs 3 --lr-schedule "1e-5@0" --warmup-steps 0 --text-weight-schedule "2@0"` (exact resume: optimizer state + sorted-batch skip; back up `model_best` first — the extension process restarts best-chrF tracking). Total 129,387 steps ≈ 10.5 h. **Resume gotcha (bug found+fixed 2026-08-03):** `optimizer.load_state_dict` used to restore the checkpoint's param groups including the old `points` LR schedule, silently overriding `--lr-schedule` (first extension attempt ran at 2e-5, not 1e-5); `train_lora.py` now reasserts the new run's schedule after resume — always verify the logged `lr` after any resume. Extension restarted clean from 086258 with the fix. **Better val metrics implemented 2026-08-03 (see post-mortem):** `evaluate_teacher_forced` now also logs `content_text_loss` (non-pad-only CE), `content_acc`, and `silence_score` (pad-prob mass at first content token); greedy log gains `nonempty_chrf`. Offline rescore of the saved greedy dirs shows nonempty_chrf held 12–16 through the epoch-2 collapse while corpus chrF fell to 3.8 — the failure is silence, not translation quality. **A/B caveat (warm-start vs collapsed ckpt): ALL teacher-forced metrics, new ones included, score the collapsed model BETTER — TF conditioning on reference text can't reach the free-running silence attractor. Greedy evals remain the only collapse detector; TF metrics are overfit/adequacy trackers only.** Run history: greedy chrF collapsed to 1.2 (111/128 EMPTY, text-pad collapse) at step 9k under peak LR 5e-5 while TF val CE kept improving (pad-dominated CE masks it), recovered to 11.4 (113/128 nonempty) by 18k — transient hot-phase dip, phase-1-style; if a future warm-start collapses AND doesn't self-recover, restart at peak ≤ the prior run's final LR (2e-5). **Run CLOSED 2026-08-03, box destroyed (instance 46610226).** 2-epoch result peaked at chrF 13.0 @ 36k, below warm-start 19.6; the epoch-3 flat-1e-5 extension (post bug-fix) stalled at its first read (90k greedy chrF 7.63, 45/128 nonempty) and was stopped per user decision. **Full analysis + outcome table + next-run recipe: `docs/phase2_postmortem.md`** — next run = from scratch on the full 1,114 h with the proven phase-1 schedule (lr 1e-4→3e-5@50%, tw 5→2@60%, warmup 500, 2 epochs, batch 16, ~6 h/$15); add real VI speech before more Kokoro hours. Artifacts on the HF **model** repo: `phase1/model_step055284.safetensors`, `phase2/{model_best_2ep,model_final_2ep}.safetensors` + `best_2ep.json` + `run_logs.tar.zst` (all logs + greedy predictions), root `model/trainer_step090000` = last resume pair. FLEURS caches+pairs are now also on the **dataset** repo (`fleurs_cache.tar.zst`, untar in `finetune/`) — dataset repo is fully self-contained.
- **Speed bench (2026-08-02, same box): best config = batch 16 + causal-SDPA flash patch + torch.compile → 0.281 s/step = 1.4× phase-1 throughput; both patches are now DEFAULT-ON for CUDA in `finetune/common.py`** (torch 2.13 fixed the 2.12 autograd break; opt out with `NO_TORCH_COMPILE=1`). Batch-8 baseline 0.196 s/step — NVL == phase-1 SXM per-step; the NVL win is 94 GB (batch 16 fits, 24 OOMs; single GPU: rent whichever is cheaper, SXM only for multi-GPU NVLink). Loss path now accumulates on-GPU and syncs only at `--log-every` (~1% on CUDA, essential on principle; `s/step` is logged). `HIBIKI_FRAME_BUCKET=1` is 1.6× WORSE on CUDA (per-shape autotune churn) — keep default 32.
- **Pod bootstrap runbook (phase-1 instance 46159865 destroyed 2026-07-29; fresh box ≈ 30 min). Phase-2 pod: CUDA 13.2 driver + torch 2.13** — after the moshi install below, re-force `uv pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu132` (replaces the phase-1 `torch==2.12.1` cu126 pin; rest of runbook unchanged). Cache-chunk tars extract shard files FLAT — untar inside `finetune/cache/phomt_stream/`. Phase-1 box: vast.ai H100 SXM 80 GB, 400 GB disk, driver 570 = CUDA ≤12.8. `source /venv/main/bin/activate`; `uv pip install moshi==0.2.13 sentencepiece sphn sacrebleu datasets soundfile hf_transfer` **then re-force the torch install** (moshi pins torch<2.10 and downgrades it every time). `git clone` repo → `/workspace/hibiki-zero`; `.env` with HF_TOKEN; `export HF_HUB_DISABLE_XET=1 HF_HUB_DOWNLOAD_TIMEOUT=120`. Weights: `config.json` + LM + mimi + tokenizer from `kyutai/hibiki-zero-3b-pytorch-bf16` → `weights/`. Restore training state from `huybik/hibiki-zero-vi-full-sft`: `mimi_caches.tar.gz` → untar into `finetune/` (recreates `cache/` + `pairs/`), and for exact resume `model_step055284.safetensors` + `trainer_step055284.pt` → `finetune/runs/vi_full/` then `--resume-checkpoint`; for phase-2 warm-start use `--init-adapter model_step055284.safetensors` instead. Long jobs: `nohup bash -c "source /venv/main/bin/activate && cd /workspace/hibiki-zero && <cmd>" > /workspace/x.log 2>&1 &` (plain nohup after `&&` loses env/cwd); never `pkill -f` a pattern contained in your own ssh command line. Launch `finetune/hf_sync.py <run_dir> huybik/hibiki-zero-vi-full-sft` alongside training.

## PhoMT synthetic speech (`training-data/`)
- Full history is in `docs/vieneu_optimizations.md`; the published artifact is HF `anquachdev/PhoMT-en-vi-speech`, consumed by `finetune/fetch_phomt.py`. The pipeline runs in its own `phomt-data` env and writes under `PHOMT_DATA_DIR`.
- **Concurrent EN+VI throughput is GPU-saturation-limited at VI ~33× / EN ~40–50× (96k tranche ≈ 5.3 h); solo benches (VI 53×, EN 70×) don't survive concurrency.** E1–E14 profiling closed the scheduling levers: extra VI workers, EN worker count, CPU-offloaded codec (subprocess ~40×/28× — too slow at real batch shapes), and threaded GPU decode (torch MPS forbids cross-thread encoding) all lose; the codec stays GPU-synchronous with a shared uniform-length attention mask. The CUDA graph path uses batch 128 and still needs a CUDA smoke test. A free-threaded CPython 3.14 re-check (repo `.venv314t`, GIL off) gave numbers identical to GIL py3.12 — the GIL was never the limiter; the CPU codec itself (~56× one thread vs ~110× LM pace) is (see `docs/vieneu_optimizations.md`).
- **Silent-wav mechanism #2 (new with today's stack, not the old NaN bug):** under the Phase-3 + Core ML-EN memory pressure, Metal silently emits all-zero rows for ~0.4% of MPS codec decodes (long-T biased). Every decoded row is now gated (finite + nonzero) with automatic CPU-clone rescue; tonight's 232 hits regenerated. The 96k-row 249600–345599 tranche scanned fully clean (safe to upload).
- Kokoro's iSTFTNet decoder runs through Core ML/Metal on macOS at ~70–75× aggregate with 7 workers; non-macOS retains 10 compiled-CPU workers. The 60 s decoder-frame bound covers the observed 49.75 s outlier. Core ML packages are keyed by conversion schema, dependency versions, and exact weights, built under an interprocess lock, and atomically published before each worker opens its own model instance.
- **Timbre-matched voices (polished 2026-07-31):** VI pool = 40 voices (12 presets + 28 VIVOS clones) after `qa_vi_voices.py` QA — PhoWhisper-small CER + clone-fidelity + speech-rate scorecard (`voice_bank/vi_voice_qa.tsv`), 8 voices cut via `VI_VOICE_BLOCKLIST` (Xuân Vĩnh/Quang Sơn/SPK22 unstable CER confirmed on a second pass; 5 draggers <12 chars/s). Each row's EN voice derives from its VI voice via `voice_bank/vi_to_en_voices.json`, built by `match_voices.py`: both sides embedded with VieNeu's 192-d speaker encoder **from synthesized calibration audio** (VI wavs come from qa_vi_voices.py — raw-ref embeddings mismeasured the clones), nearest same-gender cosine over a 34-candidate EN grid = 7 Kokoro voices + same-gender pair blends at 25/50/75 weights (`"a:0.75,b:0.25"` names resolved by `KokoroTTS.resolve_voice`, speeds = weighted mean). Worst match cos 0.03→0.17, best 0.64; ~14 distinct EN timbres in use. Pool changes shift VI RNG picks → affected rows regenerate. Rerun qa → match after changing pools/bank; audition pairs in `preview_samples/voice_match/`.
- **Campaign COMPLETE (2026-07-31):** Hub = 696,243 rows ≈ 1,228 VI-h (goal ~1,200 incl. 200 h safety). Tranche 3 fully generated + uploaded in 4 validated chunks (each: silence scan + ratio band 0.4–1.8, outliers regenerated once then dropped — 6,076 final drops). Upload now packs shard N+1 during shard N transfer and batches 5 shards/commit (the Hub rate-limits repo commits to 128/hour; single-shard commits at <0.6 min/shard trip it). All campaign wavs were deleted from /Volumes/data 2026-08-02 after Hub verification; the Hub parquet (mirrored to /Volumes/data/huggingface) is the source of truth. **Voice-scheme split:** only tranche 3 (~357k rows, 51%) has timbre-matched EN; the older ~339k rows (~95% sampled mismatch) keep the old independent EN voice pick — acceptable for robustness, retrofit only if evals show voice-consistency weakness (see `training-data/RETROFIT_PLAN.md`). Before EVERY upload.py push (including partial mid-generation ones): silence scan + ratio check (band 0.4–1.8), regenerate suspects once (batch-4 VI fallback). Upload is resumable via Hub-side upload-state.json; partial pushes of completed pairs can run in parallel with generation (merge EN worker manifests first). Changing worker counts between resumes requires merging old worker manifests and deleting stale worker CSVs. **Pause = `pkill -STOP -f pipeline.py`, resume = `pkill -CONT`** (instant, zero loss, RAM stays resident); hard-kill only before reboot/config changes — per-batch manifest appends cap that loss at ≤1 batch/worker + model reload. `HF_TOKEN_AN` in `code/.env` = dataset owner's token.
- **Dataset Viewer repair (2026-08-05, pending Hub run):** existing 500-row audio shards have one ≈493 MB row group, over the Viewer’s 300 MB scan cap. `upload.py` now writes 100-row groups with Parquet page indexes; run `training-data/repair_dataset_viewer.py --pilot`, then `--apply`, to resumably rewrite every published shard in place (remote `dataset-viewer-repair-state.json`). README YAML config is already correct (`default` → `data/train-*`).

## Gotchas
- Input must be FR/ES/PT/DE. `sphn.read(sr=24000)` resamples on load, so 16 kHz wavs run directly.
- `nn.quantize` predicate must skip modules whose last dim isn't divisible by 32 (Hibiki-M conditioners).
- HF xethub CDN times out: `HF_HUB_DISABLE_XET=1 HF_HUB_DOWNLOAD_TIMEOUT=120`.
